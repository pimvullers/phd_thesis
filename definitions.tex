
% Page size: 17x24 cm (standard thesis format)
%\setlength{\paperwidth}{17cm}
%\setlength{\paperheight}{24cm}

% Margins:
%\setlength{\topmargin}{2cm}
%\setlength
%\setlength{\oddsidemargin}{2.2cm}
%\setlength{\evensidemargin}{1.8cm}


\usepackage[papersize={17cm,24cm},margin=2cm,bindingoffset=2mm,twoside]{geometry}

%\usepackage[a4,center]{crop}\crop[frame,noinfo]
\usepackage[export]{adjustbox}

\usepackage{fancyhdr}\pagestyle{plain}

\usepackage{imakeidx}
\makeindex[intoc]

\usepackage[british]{babel}

%%% Basic support for math and graphics
\usepackage{amsmath,amsfonts,graphicx,caption,subcaption,wrapfig}

%%% Support for (clickable) references
\usepackage[hidelinks,backref=page,pdftex,
            pdfauthor={Pim Vullers},
            pdftitle={Efficient Implementations of Attribute-based Credentials on Smart Cards},
            pdfsubject={Attribute-based Credentials},
            pdfkeywords={attributes, credentials, smart cards}
	    ]{hyperref}

\renewcommand*{\backref}[1]{}
\renewcommand*{\backrefalt}[4]{%
  \ifcase #1 (Not cited)%
  \or (Cited on page~#2)%
  \else (Cited on pages~#2)%
  \fi
}

%%% Load xcolor package used by colortbl before footnote to avoid issues
%\usepackage{xcolor}

%%% Support for footnotes in tables using savenotes environment
\usepackage{footnote}
\makesavenoteenv{tabular}
\makesavenoteenv{description}

%%% Proper formatting of pseudo code using algorithm environment
\usepackage[chapter]{algorithm}
\usepackage[noend]{algpseudocode}

% Print 'for each' instead of 'for all'
\renewcommand{\algorithmicforall}{\textbf{for each}}
\renewcommand{\algorithmicreturn}{\State \textbf{return}}
\newcommand{\LineComment}[1]{\Statex\(\triangleright~\){\it #1}}

%%% Get proper underscores in text without the need to escape them
\usepackage[T1]{fontenc}
\usepackage{lmodern}

\usepackage{multicol}
\usepackage{multirow}

\include{macros}
